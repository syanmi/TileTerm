# TileTerm プロジェクトについて

このワークスペースは、任意のコンソールアプリケーションを画面分割で同時に操作できる、Windows 向け C# デスクトップターミナルツール「TileTerm」の開発プロジェクトである。

## バージョニング

**バージョンは`src/TileTerm/TileTerm.csproj`の`<Version>`の1か所だけ**(`AssemblyVersion`/`FileVersion`は.NET SDKがこれから導出するので書かない)。対応する注釈付きgitタグ`vX.Y.Z`を打つ。版を上げてタグを打つ作業は`scripts\release.ps1`が行う(後述の「リリース」)。**v0.1.0**(2026-09-18)はM1〜M10時点でのタグで、リリースの仕組みができる前のものなので、GitHub Releaseは作っていない。最初の公開リリースは**v0.1.1**。

## Gitの運用

- リモートは`https://github.com/syanmi/TileTerm`(公開)。ブランチは`master`。
- **コミットメッセージ(とタグのメッセージ)にClaudeの帰属を示す行を付けない**。具体的には`Co-Authored-By: Claude ...`(共同作者トレーラー)と`Claude-Session: https://claude.ai/code/...`の2種類。GitHubのContributorsは`Co-Authored-By`のメールアドレスも数えるため、付けるとClaudeが貢献者として表示されてしまう(`git log`の作者欄には出ないので気づきにくい)。2026-09-19に、既存の全コミットとタグ`v0.1.0`からこの2種類の行を取り除いて履歴を書き換え、`master`と`v0.1.0`を強制プッシュした(ファイル内容・作者・日付・件名は変えていない。`git filter-branch --msg-filter`で該当行だけを削除し、タグは元の作成日時のまま本文を清掃して付け直した)
- **別の場所のcloneが古い履歴を持っている場合は、そこから`push`し直さないこと**(古いコミットが復活する)。作り直すか、`git fetch`して`git reset --hard origin/master`する
- 履歴を書き換えた後は、`git filter-branch`が自動で作る退避用の参照`refs/original/`(ローカルのみ)が残るので、確認後に`git update-ref -d`で消す。ブランチ一覧(`git branch`)には出ないので見落としやすい

## リリース

GitHub Releasesで配布する。パッケージは**Velopack**(.NET向けのインストーラ兼アップデータ、MIT)で作り、将来のアプリ内の自動アップデートも同じ仕組みで行う。

**Releaseに添付するもの**:

- `TileTerm-win-Setup.exe` インストーラ。ワンクリック、ユーザー単位(管理者権限不要)で`%LOCALAPPDATA%\TileTerm`に入り、スタートメニューとデスクトップにショートカットを作る。**新しいSetup.exeを上書き実行するとその場で更新される**(確認済み)。アンインストールしても`%AppData%\TileTerm`のユーザーデータは残る。固定のURL`https://github.com/syanmi/TileTerm/releases/latest/download/TileTerm-win-Setup.exe`で常に最新版を取れる
- `TileTerm-win-Portable.zip` ポータブル。展開して`TileTerm.exe`を実行する。データは展開先ルート(`TileTerm.exe`の隣)の`data`フォルダに置く(`AppPaths.cs`。`VelopackLocator.Current.IsPortable`が真のときだけ)。**更新で丸ごと入れ替わる`current`フォルダの外に置く**のが要点(中に置くと更新でデータが消える)。書き込めない場所ならAppDataに黙ってフォールバック
- `TileTerm-<版>-full.nupkg`、`TileTerm-<版>-delta.nupkg`、`releases.win.json`、`assets.win.json`、`RELEASES`: アプリ内アップデータが読む更新パッケージとフィード。差分は変わったファイルだけ(0.1.1→0.1.2のローカル検証で差分約0.2MB、全体は約63MB)
- `SHA256SUMS.txt`

**作り方の要点**:

- .NETランタイム同梱(self-contained, win-x64)。**単一exeにしない**: Velopackはファイル単位で差分を作るので、複数ファイルのままなら更新でランタイムを再取得せず小さく済む(インストール後は約140MB)。`vpk pack`のポータブル/インストーラはこの発行フォルダを包む
- **`Program.cs`が本当のエントリポイント**(csprojの`StartupObject`と、`App.xaml`を`ApplicationDefinition`から`Page`へ変えることでWPF既定の`Main`を外している)。`VelopackApp.Build().Run()`を最初に呼ぶ必要がある: インストーラ/アップデータ/アンインストーラが特殊な引数でexeを起動した時、ここで処理してUIを出さずに終了する。通常起動では即座に戻り、Velopackでインストールされていない起動(IDEや`bin\Debug`)では何もしない。`vpk pack`が`Program.Main`にこの呼び出しがあるかを検査してくれる
- **コード署名はしていない**ので初回起動でSmartScreenの警告が出ることがある(READMEに説明済み)。自動更新は「ダウンロードして実行する」経路なので、有効にする前に署名(SignPath FoundationのOSS向け無償署名など)を検討する
- **アプリ内の更新確認と自動更新は未実装**。土台(フィードと差分、`Program.cs`のフック)と、Velopackのテスト用API(`TestVelopackLocator`+`SimpleFileSource`)を使った検証(インストール済み0.1.1から0.1.2の差分を見つけ、差分だけをダウンロードして全体を復元できる)までは済んでいる。実装する時: `new UpdateManager(new GithubSource("https://github.com/syanmi/TileTerm", null, false))`で`CheckForUpdatesAsync()`→`DownloadUpdatesAsync()`→`ApplyUpdatesAndRestart()`。`IsInstalled`が偽なら何もしない。設定でオフにでき、再起動で開いているタイルが閉じることをユーザーに伝える

**リリース手順**(すべて自動):

1. `scripts\release.ps1 -Version X.Y.Z`を実行する。masterで、追跡ファイルに未コミットの変更がなく、origin/masterより遅れておらず、タグが未使用であることを確認してから、csprojの版を更新して`Release vX.Y.Z`をコミットし、注釈付きタグ`vX.Y.Z`を打ってpushする(`-NoPush`で手元までで止められる)
2. タグのpushで`.github/workflows/release.yml`が動き、`scripts/build_release.ps1 -Tag vX.Y.Z`でパッケージを作ってReleaseを公開する。`-`を含む版(`1.0.0-beta.1`)はPre-releaseになる。**タグとcsprojの`Version`が食い違うとスクリプトが失敗する**
3. Actionsの「Run workflow」(`workflow_dispatch`)で、Releaseを作らずにパッケージだけ作って試せる(成果物はワークフローのアーティファクトに残る)。`ci.yml`は毎push/PRで、ビルドに加えて同じパッケージ作成を走らせる(公開はしない)ので、パッケージ作成の破損はリリース前に分かる

**リリースノート**は手書きしない。`scripts/release_notes.ps1`が前のタグからのコミット件名を並べて作る(このプロジェクトはPRを使わず`master`へ直接コミットするため、GitHubの自動生成ノートはほぼ空になる。`Release v`で始まるコミットは除く)。同じ文がフィード(`releases.win.json`)にも入り、アプリ内アップデータの表示に使える。**コミット件名を読みやすく書くことがそのままノートの質になる**

**ローカルで再現**: `dotnet tool restore`(`vpk`を入れる)→`scripts\build_release.ps1 -SkipDeltaBase`で`artifacts/`(gitignore済み。アップロード対象は`artifacts/release/`)にできる。`-DeltaBaseDir <前のリリースのフォルダ>`で差分をローカルで試せる。実際のリリースでは`vpk download github`で前のReleaseの全体パッケージを取得して差分を作る(最初のリリースでは何も無くても警告だけで成功する)。`vpk`は`.config/dotnet-tools.json`で版を固定したローカルツールで、アプリが参照する`Velopack`ライブラリと同じ版に揃える。スクリプトは**ASCIIのみ**で書く(BOMなしUTF-8はWindows PowerShell 5.1が文字化けさせる)。`.gitignore`に`build/`があるので、パッケージ関連のフォルダ名に`build`は使わない

**その他**: `THIRD-PARTY-NOTICES.md`は依存ライブラリを増減したら更新する(バイナリ配布ではMITの著作権表示の同梱が必要)。`reference/`(JetBrains製品のスクリーンショット)は第三者の著作物なのでgitignoreにしたが、過去のコミットには残っている

## コンセプト・狙い

- コンソールアプリを操作するための Terminal ツール
- [RLogin](https://osdn.net/projects/rlogin/) を参考に、1つのウィンドウを複数のペインに分割し、複数セッション・複数のコンソールアプリを1つのウィンドウ内で同時に操作できるようにする
- C# のデスクトップアプリとして開発する
- ツールの設計として、任意のコンソールアプリであれば設定次第で何でも操作・制御可能なものを目指す
  - 設定で「プロファイル」としてコンソールアプリの .exe パスと起動引数を指定できる
  - ツール上から新規セッションを開始する際は、選択したプロファイルに応じた画面が起動する
  - これにより、ターミナルツール自体は「任意のコンソールアプリ」に対して変更を加えずに使えるようにする
- 対応させたいコンソールアプリの例: コマンドプロンプト(cmd.exe) / PowerShell / Git Bash / Claude Code CLI (claude) など

## 用語の注記

- 会話の初期段階では起動設定を「プロンプト」と呼んでいたが、一般的な用語法では「プロファイル(profile)」と呼ぶのが適切（Windows Terminal の `profiles.json` 等が典型例）。今後のドキュメント・実装では「プロファイル」の語を使う。

## 技術スタック

- C# / .NET 9 (`net9.0-windows`)、SDK は `global.json` で `9.0.300` に固定（マシンに他バージョンのSDKが混在しているため）
- UI フレームワークは **WPF**
- ソリューション構成:
  - `TileTerm.sln`
  - `src/TileTerm/TileTerm.csproj` — WPF アプリ本体（今のところ唯一のプロジェクト。将来ペイン分割やプロファイル管理が増えたらライブラリに切り出すことを検討）

### ターミナル側の実装方式（重要な技術選定）

任意のコンソールアプリ（cmd.exe だけでなく PowerShell や claude のような対話的 TUI アプリも含む）を正しく動かすには、単純な標準入出力リダイレクトではなく **本物の仮想端末(pty)** が必要という判断から、以下の2つの NuGet パッケージを組み合わせている。どちらも作者は同じ (Tom Laird-McConnell / tomlm)。

- **[Porta.Pty](https://github.com/tomlm/Porta.Pty) 1.0.7** — 子プロセスを Windows の ConPTY 経由で起動する pty ライブラリ。`PtyProvider.SpawnAsync(PtyOptions, ct)` → `IPtyConnection`（`ReaderStream`/`WriterStream`/`Resize`/`ProcessExited`/`ExitCode`）というAPI。
  - 注意: このパッケージの **2.0.0 以降は `.NET 10` 必須**になっている。本プロジェクトは .NET 9 のため、意図的に `netstandard2.0` 向けの **1.0.7** に固定している。将来 .NET 10 に上げるならバージョンアップを検討して良い。
- **[XTerm.NET](https://github.com/tomlm/XTerm.NET) 1.2.0** — VT100/ANSI エスケープシーケンスを解釈する「ヘッドレス」な仮想端末エンジン（xterm.js の C# 移植）。UI描画は一切持たず、`Terminal.Buffer` からセル(`BufferCell`: 文字・色・属性)を読み取って自前でレンダリングする方式。
  - こちらも **2.0.0 以降は `.NET 10` 必須**なので、同じ理由で **1.2.0** に固定している。
  - キー入力は `Terminal.GenerateKeyInput(Key, KeyModifiers)` / `GenerateCharInput(char, KeyModifiers)` を使ってエスケープシーケンス文字列を生成し、それを UTF-8 バイト列として `IPtyConnection.WriterStream` に書き込む。この2つのパッケージ以外に自前でVT100パーサやConPTYのP/Invokeを書く必要はない。

パッケージのバージョンを上げる際は、README上の説明だけでなく **NuGetパッケージに同梱された README.md や、リフレクションで実際の公開APIを確認してから**変更すること（メジャーバージョン間でAPIやターゲットフレームワークが変わっているため）。

### レンダリング方式

`TerminalCanvas`（`FrameworkElement` を継承したカスタムコントロール、`src/TileTerm/Terminal/TerminalCanvas.cs`）が `XTerm.Terminal.Buffer` を1セルずつ読み取り、`DrawingContext.DrawText` で等幅フォント(Consolas)のグリッドとして描画している。256色/RGBカラーの変換は `AnsiPalette.cs` に実装。現状は「まず正しく動く」を優先したシンプルな逐セル描画で、パフォーマンス最適化（同一属性の連続セルをまとめて1回のDrawTextにする等）は未実施。

## アーキテクチャ（M2時点）

### ペイン分割（`PaneTree.cs` / `PaneManager.cs`）

RLoginのような「1ウィンドウを好きなだけ分割する」レイアウトは、二分木で表現している。

- `LeafNode` = 1枚の実ペイン（`TerminalPaneControl` を保持）
- `SplitNode` = 領域を2つに分けるノード（`Direction: Right/Down`、`First`/`Second`の子、実際の分割線を描画する`Grid`(+`GridSplitter`)を保持）
- `PaneManager` がこの木を操作する唯一の場所: `Split(leaf, direction, profile)` で対象ペインを新しい`SplitNode`に置き換え、`Close(leaf)` で対象を取り除いてその兄弟を親の位置に昇格させる（木の畳み込み）

**ハマった実装バグとその教訓**: `Split`/`Close`共通の「子ノードを差し替える」ヘルパーの中で毎回「古い要素をVisualツリーから外す」処理を呼んでいたところ、`Split`では呼び出し側が対象を*先に*新しいGridへ move 済みだったため、この内部の「外す」処理が「新しい(正しい)親」から誤って外してしまい、該当ペインが描画されない不具合が起きた（分割で3ペイン目を作ると、そのうち1ペインが真っ黒になる形で顕在化）。**WPFの要素は同時に1つの親しか持てないため、「要素を動かす」コードを書くときは、どの時点で・どの親から・何が誰の責任で外すのかを一箇所に集約し、二重に外そうとしないこと。**動作確認（実機でのペイン分割・入力テスト）をしなければ気づけなかった類のバグなので、UIレイアウトを操作するコードは必ずビルド後に実際に操作して確認する。

### プロファイル管理（`ProfileStore.cs` / `SettingsWindow.cs`）

- 保存先: `%AppData%\TileTerm\profiles.json`（JSON、`System.Text.Json`）。プロファイル本体に加えて既定プロンプトID・お気に入りIDリストも保存する（詳細はM5参照）
- 初回起動時、このマシンに実在するアプリだけを自動検出してデフォルトプロファイルを作る: Command Prompt(常時) / Windows PowerShell(`System32`にあれば) / Git Bash(`C:\Program Files\Git\bin\bash.exe`があれば、`--login -i`付き) / Claude Code(`PATH`上に`claude`があれば)
- 設定UI(`SettingsWindow`)からプロファイルの追加・編集・削除、既定/お気に入りの設定が可能。タイトルバーの歯車アイコンから開く
- **ハマったポイント**: npmでインストールされるCLI等は `.exe` ではなく `.cmd`/`.bat` シムであることが多く、ConPTY(CreateProcess)は`.cmd`/`.bat`を直接起動できない。`ProcessLaunchResolver.cs` で拡張子が`.cmd`/`.bat`なら自動的に `cmd.exe /c <path> <args>` に置き換えることで対応している

### カスタムタイトルバー（M3）とペイン操作の導線

OSデフォルトの装飾（`WindowStyle="None"` + `WindowChrome`）を捨て、VSCodeのようなモダンなアプリに合わせた自前のタイトルバーにしている（`MainWindow.xaml`）。左から: アイコン → 分割→/分割↓/タイルを閉じる/設定(歯車)ボタン → お気に入りバー(可変長) → (ドラッグ可能な余白+タイトル文字) → 最小化/最大化/閉じるボタン、という並び。

- **分割→/分割↓/タイルを閉じる**は「今アクティブなペイン」に対して働く（`PaneManager.SplitActive`/`CloseActive`）。M5以降、分割ボタンは**常に既定プロンプトで即座に分割**する（メニューは出ない）。既定以外のプロファイルで分割したい場合はお気に入りバーを使う（詳細はM5参照）
- 各`TerminalPaneControl`自身のヘッダーは、プロファイル名の表示 + **更新**(このタイルだけセッションを再起動)+ **×**(このタイルを閉じる)の3つだけに絞った。分割操作はすべてメインウィンドウのタイトルバー側に統一している
- ウィンドウの閉じるボタンを押すと確認ダイアログ（「開いているすべてのタイルを閉じます。よろしいですか？」）が出て、OKで全セッションを`ShutdownAll()`してから終了、キャンセルなら`e.Cancel = true`で閉じるのをやめる
- `WindowChrome`使用時の既知の落とし穴（最大化時にコンテンツの端が数px切れる問題）は、M6で`WindowMaximizeFix.cs`（`WM_GETMINMAXINFO`フック）による正攻法に置き換え済み。詳細はM6参照

## マイルストーン進捗

- ✅ **M1: 単一ターミナルが動くところまで**（2026-09-08 完了）
  - `dotnet new sln` / `dotnet new wpf` でプロジェクト作成
  - ConPTY(Porta.Pty) + XTerm.NET で cmd.exe を1セッション起動し、実際にキー入力・画面描画（日本語ディレクトリ名を含む `dir` コマンド出力等）が正しく動作することをビルド後に実機で確認済み
  - 起動するプロファイルは `ProfileDefinition.Default`（cmd.exe固定）にハードコードされている

- ✅ **M2: コンセプト機能一式**（2026-09-08 完了）
  - ペイン分割UI（右/下split、ネスト分割、close時の木の畳み込み）を実装
  - プロファイル設定UI＋`profiles.json`永続化、初回起動時のデフォルトプロファイル自動検出を実装
  - 実機で以下を確認済み: 右split(Command Prompt + Git Bash、互いに独立してキー入力が届く)、既存ペインの下split(3ペインのネスト表示)、ペインを閉じたときのレイアウト再構成、設定画面でのプロファイル一覧表示・選択・フォーム反映、**Claude Code CLI (`claude`) を実際にペイン内で起動し、TUIの確認画面が正しく描画されることまで確認**（コンセプトで名指しされていた4つの対象アプリ: cmd/PowerShell/Git-bash/claude、すべて動作確認済み）
  - 動作確認はSendKeysではなく`PrintWindow`(DWM合成中でも取得できる)によるキャプチャと、UI Automationの`InvokePattern`によるボタン操作で行った。**このデスクトップ環境では`SetForegroundWindow`や合成`SendKeys`によるウィンドウのフォアグラウンド化が信頼できず**（他のウィンドウにフォーカスを奪われる／`GetWindowRect`で取れる座標へ実際にクリックしても別ウィンドウに当たる等）、UI Automation経由の操作とPrintWindowでのキャプチャの組み合わせが安定して機能した。今後この手のUI自動確認をする際はこの方式を使うこと。

- ✅ **M3: モダンなカスタムタイトルバー化**（2026-09-08 完了）
  - OS標準のタイトルバーを廃止し、`WindowChrome`ベースの自前タイトルバーに変更（アイコン/分割→/分割↓/タイルを閉じる/設定/最小化/最大化/閉じる）
  - 各ペインのヘッダーから分割ボタンを削除し、「更新(再起動)」ボタンを追加。分割操作はタイトルバー側に一本化
  - ウィンドウを閉じる際の確認ダイアログ（OK/キャンセル）を追加
  - 実機で以下を確認済み: タイトルバーからの右split、ペインの更新ボタンによるセッション再起動（再起動前に入力した内容が消え、新しいプロセスの初期画面に戻ることを確認）、タイトルバーのタイルを閉じるボタン（アクティブペインが閉じてレイアウトが再構成される）、閉じる確認ダイアログのキャンセル/OK双方の挙動、最大化（タスクバーとの重なりなし）
  - **ハマったポイント**: `更新`ボタンの動作確認で最初「効いていないように見えた」が、原因はテスト側（UI Automationでのボタン特定）が別ペインの同名ボタンを誤って掴んでいたことだった。同じラベルのボタンが複数ペインに存在する場合、名前だけでなく`BoundingRectangle`の座標も突き合わせて対象を一意に特定すること

- ✅ **M4: ボタンのアイコン化とタイトルバーの見た目調整**（2026-09-09 完了）
  - 分割→/分割↓/タイルを閉じる/更新/×（タイル閉じる）を、テキストラベルから`Icons.cs`の自前ベクターアイコンに置き換えた。アイコンフォント（Segoe Fluent Icons等）は使わず、`Path`/`Rectangle`/`Line`をコードで組み立てて描画している——これは特定フォントの有無に依存せず、どの環境でも確実に同じ見た目になるようにするための意図的な選択（"更新"アイコンだけは循環矢印の正確なパス描画が難しいため、代わりに一般的なUnicode文字`↻`を使用）
  - メインウィンドウの最小化/最大化/閉じるボタンを、他の汎用ボタンと共通のPadding依存サイズから切り離し、`WindowControlButton`という専用スタイル（幅46px・タイトルバー全高）に変更。合わせてタイトルバー自体も32px→34pxに、アイコンは実際の見た目を見ながら調整し、Windows標準アプリのタイトルバーに近い比率になるようにした
  - アイコンのみのボタンはテキストが無くUI Automationから名前で探せなくなるため、`AutomationProperties.Name`を明示的に付与した（副次効果としてスクリーンリーダー等のアクセシビリティも改善）
  - 実機で全アイコンの表示（拡大キャプチャで確認）、最大化時のアイコン切り替え（□→重なった2つの四角）、最大化時の左端クリップ問題が再発していないことを確認済み

- ✅ **M5: 設定ダイアログ・既定プロンプト・お気に入りバー**（2026-09-10 完了）
  - `ProfileSettingsWindow`を`SettingsWindow`に発展させ、左に「プロンプト」カテゴリのサイドバーを追加（カテゴリはまだこれ1つだが、増やせる構造にしてある）
  - プロンプトの編集項目に「表示アイコン」を追加（`ProfileDefinition.Icon`: 絵文字や"PS"のような短い文字列。画像アセットは使わず文字列のみ——アイコンフォント同様、環境依存を避けるため。空欄なら名前の頭文字にフォールバック）
  - `ProfileStore`が「既定プロンプト(`DefaultProfileId`)」と「お気に入り(`FavoriteProfileIds`、複数・順序あり)」を`profiles.json`に永続化するようになった。**JSONの形が配列からオブジェクトに変わったため、この変更前に実際にこのマシンで生成されていた旧形式ファイルを読み込んで新形式へ移行するコードを`ProfileStore.Load()`に実装し、実機で実際に手元の旧ファイルが壊れずアップグレードされることを確認した**（既存ユーザーデータを失わない設計は重要な注意点）
  - タイトルバーの分割→/分割↓ボタンの挙動を変更: これまでの「プロファイル選択メニューを出す」から「常に既定プロンプトで即座に分割する」に変更した。他のプロファイルで分割したい場合はお気に入りバーを使う
  - タイトルバーの設定ボタンの右に、お気に入り登録したプロンプトのアイコンボタンが並ぶ「お気に入りバー」を追加。**ダブルクリックで縦分割(左右に並ぶ分割)、右ダブルクリックで横分割(上下に並ぶ分割)**でそのプロンプトを起動する（日本語の「縦分割/横分割」はvimの`:vsplit`/`:split`と同じ慣習で、分割線の向きを指す言葉として扱った）
  - **ハマったポイント**: お気に入りボタンを最初`Border`で自作したところ、UI Automationから全く見つけられなかった。WPFは`Border`のような素のレイアウト要素をControl view上のコントロールとして扱わない（`IsControlElement=false`)ため。`AutomationProperties.IsControlElement`のようなWPF側の属性は存在しない（UWP/WinUIの概念で、WPFではAutomationPeerのサブクラス化が必要）ので、実用上は素直に`Button`を使う方が正しい。タイトルバーの`TitleBarButton`スタイルを`FindResource`で流用することで、見た目の統一とアクセシビリティの両方を素直に解決できた
  - 実機で以下を確認済み: 設定画面でのプロンプト一覧（既定=⭐、お気に入り=♥のマーカー表示）、既定の切り替え、お気に入りの登録/解除、設定を閉じた後のお気に入りバーの反映、お気に入りボタンの左ダブルクリック（縦分割）と右ダブルクリック（横分割）でそれぞれ正しい方向に指定プロンプトが起動すること、タイトルバーの通常の分割ボタンが既定プロンプト(Command Prompt)で即座に分割されること（メニューが出ないこと）

## 動作確認用スクリプト

`scripts/build_and_run.bat` をダブルクリック（またはコマンドラインから実行）すると、ビルド〜アプリ起動までを1回で行う。ユーザー自身がClaude Codeを介さずに最新のビルドを動作確認できるようにするためのもの。

- 実行内容: `dotnet build TileTerm.sln -c Debug` → 成功したら `src/TileTerm/bin/Debug/net9.0-windows/TileTerm.exe` を起動 → アプリを閉じるとexit codeを表示して`pause`
- **バッチファイル内のメッセージは意図的に英語(ASCII)にしている**: 日本語(UTF-8)で書いたところ、cmd.exeの既定コードページ(Shift-JIS)との不一致でコマンド自体が文字化けし、`chcp 65001`を先頭に置いても解決しなかったため。今後このファイルを編集する際も日本語は使わないこと。

- ✅ **M6: 見た目の不具合修正一式**（2026-09-17 完了）
  - **最大化時の余白**: M4で入れた「`SystemParameters`ベースのマージン加算」ハックが過剰補正になり、タイトルバーの上に隙間ができてしまっていた。`WindowMaximizeFix.cs`で`WM_GETMINMAXINFO`をフックし、最大化サイズ/位置をモニタの作業領域(`GetMonitorInfo`の`rcWork`)に直接合わせる正攻法に置き換えた。これでマージンハック自体が丸ごと不要になった（`MainWindow.xaml.cs`の`RootGrid.Margin`操作は削除済み）
  - **設定ダイアログのダークテーマ化**: `SettingsWindow`に色を一切指定していなかったため既定の白背景になっていた。メインウィンドウと同じダークパレットを明示的に適用。さらにOSネイティブのタイトルバー帯（アイコン・最小化/最大化/閉じるの部分）も`DarkTitleBar.cs`(M18で`TitleBarTheme.cs`に改名)で`DwmSetWindowAttribute(DWMWA_USE_IMMERSIVE_DARK_MODE)`を呼んでダーク化した（`WindowStyle="None"`で自前タイトルバーを持つMainWindowには不要、OS標準タイトルバーを使う子ウィンドウ向けの仕組み）
  - **タイトルバー左上のアイコン**: Unicode文字("&#9638;" ▦)を使っていたが、位置がずれて見える不具合があったため、`Icons.App()`として2x2のタイル(自前のRectangle描画)に置き換えた。これでアプリの他の全アイコンと同じ「フォントに頼らずコードで描画する」方針に統一された
  - **お気に入りボタンの文字が見えない**: `BuildFavoriteButton`が生成する`TextBlock`に`Foreground`を指定しておらず、既定の黒文字がダークな背景と被って見えなくなっていた。`Foreground = Brushes.Gainsboro`を明示。**教訓: ダークテーマのUIでは、要素を追加するたびに前景色を明示的に指定すること。既定値(黒)は背景と被って気づきにくい形で消える。**
  - **タイルの境界の見直し**: 3点セットで対応。(1) `PaneHost`に`Margin="4"`を追加し、タイルが常にウィンドウ端から数px離れるようにした。これにより①ウィンドウ端に接したペインの文字が欠けて見える問題と、②アクティブペインの青枠ハイライトがウィンドウ端で見切れる問題の両方が同時に解消した（どちらも「要素がウィンドウの実ピクセル境界ぴったりに接している」ことが原因だった）。(2) `PaneManager.MakeSplitter`のGridSplitterを、5pxの単色バーから「幅5pxの透明な当たり判定の中に1px の線を`ControlTemplate`(`FrameworkElementFactory`)で中央配置」する方式に変更し、太く見えないようにした
  - 実機で確認済み: 最大化してもタイトルバー上に隙間がなくタスクバーとも重ならないこと、設定ダイアログの内容とOSタイトルバー帯の両方がダークになっていること、タイトルバー左上のタイルアイコンが正しく中央揃えで表示されること、お気に入りボタンの頭文字がはっきり見えること、2分割時に左ペインの文字が欠けずアクティブ枠が全辺表示され、分割線が細くなっていること

- ✅ **M7: タイル境界の見た目の再修正**（2026-09-17 完了）
  - M6の指摘への追加フィードバック。3点あったがいずれも根は同じ「アクティブ枠が内容の上に重ねて描画されていた」ことだった:
    1. **枠がぼやける**: `UseLayoutRounding`を`MainWindow`で有効化していなかったため、細い線・枠がデバイスピクセルにスナップされずアンチエイリアスでぼやけて見えていた。`UseLayoutRounding="True"` `SnapsToDevicePixels="True"` をWindowに設定（子孫に継承される）。`GridSplitter`とその内部の線`Border`にも明示的に`SnapsToDevicePixels`を設定
    2. **青枠がプロンプト内容を潰す**: `TerminalPaneControl`のアクティブハイライト用`Border`(`BorderThickness=2`)が、ヘッダー/ターミナルCanvasの真上に同じ矩形で重ねて描画されていたため、非アクティブ時は問題ないが、アクティブになるとその2pxがヘッダーやターミナル文字の外周に重なって表示が欠けて見えていた。ヘッダー+Canvasを`Margin=2`(=`BorderInset`定数、枠の太さと同じ値)のインナーGridに包み、外側の実際の枠はその2pxの余白(常に確保されている、非アクティブ時は透明)にのみ描画するよう構造変更。**これでコンテンツと枠は物理的に重ならなくなった**
    3. **青枠が内側にずれて見える**: 実体は(2)と同じ現象（枠が内容を侵食していたことで、枠自体がタイルの本当の境界より内側にあるかのように錯覚していた）。(2)の修正で解消
  - 実機で拡大キャプチャして確認: アクティブペインの青枠が完全に鮮明(ぼやけなし)、ヘッダー文字・ターミナル文字が枠と重ならず全て表示され、枠とコンテンツの間に一定の余白があることで違和感のない自然な見た目になっていることを確認済み

- ✅ **M8: 最大化時のタイトルバードラッグ不良・タイル間余白の調整**（2026-09-17 完了）
  - **最大化時にタイトルバーを掴めないことがある**: `WindowChrome`は`ResizeBorderThickness`分のヒットテスト領域を常にウィンドウの全辺（上端含む）に対して計算するため、最大化中でも本来リサイズ不要なはずのウィンドウ上端の数pxが「リサイズ枠」として扱われ、そこをクリックするとキャプション(ドラッグ)ではなくリサイズ操作と解釈されてクリックが無視されることがあった。`StateChanged`で`WindowChrome.ResizeBorderThickness`を最大化時は`0`、通常時は`4`に切り替えることで解消
  - **タイル間の余白が大きい**: M7で導入した「アクティブ枠がコンテンツと重ならないようにするための内側マージン(`BorderInset`)」が2pxで、隣接する2ペイン間では両側合わせて4px+スプリッター幅5px=最大9pxもの隙間になっていた。`BorderInset`を1pxに、スプリッター幅を5→3pxに、`PaneHost`の外周マージンを4→2pxに、それぞれ縮小。枠線は`UseLayoutRounding`のおかげで1pxでも鮮明に見えるため、視認性を落とさずに詰められた
  - 実機で確認済み: 最大化・復元のサイクルに問題がないこと（リグレッションなし）、分割時のペイン間の余白が目に見えて詰まったこと、アクティブ枠が1pxになっても鮮明に見えコンテンツと重ならないこと

- ✅ **M9: アクティブ枠の太さとコンテンツ余白の再調整**（2026-09-17 完了）
  - M8で枠を1pxまで細くしたところ「1pxは細すぎて見づらい」「コンテンツの1文字目が青枠に接している」という指摘があった。「枠の太さ」と「コンテンツの内側余白」を1つの定数(`BorderInset`)で兼用していたのが原因（枠を細くすると余白も一緒に減っていた）。`ActiveBorderThickness`(枠自体の太さ、2px)と`ContentInset`(コンテンツの内側マージン、枠の太さ+2pxの余裕)を別々の定数に分離し、それぞれ独立して調整できるようにした
  - 実機で拡大キャプチャして確認: 枠がはっきり見える太さになり、かつコンテンツ(ヘッダー文字・ターミナル文字)と枠の間に明確な隙間ができ、文字が枠に接しなくなったことを確認済み

- ✅ **M10: 自作ダークダイアログ化、ボタンのホバー/無効時テーマ不具合修正**（2026-09-17 完了）
  - **`MessageBox.Show`を廃止**: OS標準の`MessageBox`は常にOSのライトテーマで描画され、アプリのダークテーマと合わない。`MessageDialog.cs`として自作の軽量ダイアログ(`Window`+`ShowDialog`、OK/OKCancel対応)を実装し、全呼び出し箇所(メインウィンドウを閉じる確認、既定プロンプト未設定の警告、セッション起動失敗の通知、設定画面での未選択警告)を置き換えた
  - **設定ダイアログの「既定のプロンプトにする」ボタンが読めない不具合**: 原因は`Background`/`Foreground`プロパティを設定するだけでは、ボタンの既定テンプレート(OSテーマ由来)が持つホバー/押下時の背景色トリガーを上書きできていなかったこと。ホバーすると明るい背景色になるが文字色は変わらない設計のテンプレートのままだったため、組み合わせ次第で読みにくくなっていた。`Theme.cs`に、hover/pressed/disabledの全状態を明示的に制御する共通`ControlTemplate`を持つ`Theme.Button(...)`ファクトリを作り、`SettingsWindow`・`MessageDialog`の全ボタンをこれに統一。**教訓: WPFで`Background`/`Foreground`をプロパティ指定するだけでは、コントロールの既定テンプレートのInteraction(ホバー/押下/無効)トリガーまでは上書きされない。状態ごとの見た目まで保証したい場合はControlTemplate自体を用意すること**
  - 併せて`SettingsWindow`が個別に持っていたダーク配色定数を`Theme.cs`に集約し、今後追加するウィンドウ/ダイアログも`Theme.Apply(window)` / `Theme.Button(...)` / `Theme.TextBox()`を使うだけで一貫したテーマになるようにした
  - 実機で確認済み: 閉じる確認ダイアログがOSタイトルバー含め完全にダーク表示されること、「既定のプロンプトにする」ボタンがホバー時・無効時ともに文字がはっきり読めること

- ✅ **M11: 設定ダイアログのデザイン刷新（VSCode/JetBrains風）**（2026-09-18 完了）
  - それまでの設定ダイアログは機能的には問題なかったが、既定のシステムカラー・素朴な余白・装飾のないリストで「自作アプリ感」が強かった。VSCode/JetBrains(CLion等)の設定画面を参考に、以下を導入:
    - **アクセントカラー**(`Theme.Accent`、青系)をカテゴリ一覧・プロンプト一覧の選択状態、保存ボタン、入力欄フォーカス時の枠線に一貫して使用
    - `Theme.ListBox()` / カスタム`ListBoxItem`テンプレートで、選択行がOS既定の水色ではなくアクセントカラー＋白文字になるように統一
    - `Theme.TextBox()`に角丸(`CornerRadius=4`)とフォーカス時のアクセント枠線を追加
    - プロンプト一覧の上に+/−の小さいアイコンボタン(`Theme.IconButton`)を配置し、テキストの「追加」「削除」ボタンから置き換え
    - 各入力欄のラベルを小さく・控えめな色(`Theme.FgMuted`)にして、値との視覚的な階層を作った
    - 「保存」ボタンをフォーム内から**ウィンドウ下部に固定されたアクションバー**に移動し、アクセントカラーの主要ボタン(`Theme.PrimaryButton`)として「閉じる」(通常ボタン)と並べて配置。VSCode/JetBrains系の「ダイアログ全体の下端にOK/Cancel相当を固定する」構成に合わせた
    - セクション間を極太のSeparatorではなく、細い低コントラストの区切り線(`Theme.Divider`)で統一
  - 実機で、カテゴリ選択・プロンプト選択それぞれのアクセントカラーハイライト、テキスト入力欄の角丸とフォーカス時の枠線、下部アクションバーの配置、既定ボタンの無効状態の見た目を確認済み

- ✅ **M12: 設定ダイアログの密度調整（「はれぼったい」フィードバックの解消）**（2026-09-18 完了）
  - M11でアクセントカラー等を導入した際、パディング・マージン・角丸をやや大きめに設定していたため「1個1個が大きくてぼってりしている」という指摘があった。VSCode/JetBrainsの設定画面は実際にはかなり密度が高い（余白が小さい）ことを踏まえ、全体的に数値を詰め直した:
    - 角丸: 4px→3px
    - ボタンのpadding: 通常/主要ボタンとも大幅に縮小(例: 主要ボタンは14,5,14,5→11,3,11,3)、主要ボタンの`FontWeight.Medium`も外して通常の太さに
    - TextBoxのpadding: 7,5,7,5→5,3,5,3
    - リスト項目のpadding/margin: 8,6,8,6 + margin(0,1,0,1) → 6,3,6,3 + margin無し
    - フォーム内の各項目間の余白・セクション見出し・区切り線まわりの余白もそれぞれ縮小
    - ダイアログ自体のサイズも780×480→740×440に縮小
    - Button/TextBox/ListBoxに明示的に`FontSize=12`を設定して文字サイズも統一
  - 実機で拡大キャプチャし、カテゴリ/リスト項目・ボタンのサイズ感が締まって見えることを確認済み

- ✅ **M13: タイルのドラッグ&ドッキング、タイトルバークリックでのアクティブ化、設定ダイアログの可変セパレータ、プロンプト一覧のアイコン化**（2026-09-18 完了）
  - **タイルのドラッグ&ドッキング**: タイルのタイトルバーを掴んでドラッグし、別のタイルの上にドロップすると、VSCode同様に位置の入れ替え・分割方向の変更・端へのドッキングができるようにした。`TerminalPaneControl`の各インスタンスに`Guid PaneId`を持たせ、`DragDrop.DoDragDrop`でその`Guid`を運ぶ。ドロップ先タイル内での位置(中央28%を除く外側の帯)に応じて`PaneTree.DropZone`(Left/Right/Top/Bottom/Center)を判定し、ドラッグ中は該当ゾーンを青いオーバーレイでハイライトして表示先を視覚化。`PaneManager`に`SwapPanes`(中央ドロップ＝位置入れ替え)と`DockPane`(端ドロップ＝新しい分割を作ってそこに移動)を追加し、既存の`Close`が使っていた「ツリーからリーフを切り離す」処理を`DetachLeafFromTree`として共通化して再利用した
  - **タイトルバークリックでアクティブ化されない不具合の修正**: タイトルバーのクリックが、ペイン内クリックと同様に`Activated`イベントを発火させていなかった。タイトルバーの`PreviewMouseLeftButtonDown`でキャンバスに`Focus()`する処理を追加し、既存の「キャンバスがフォーカスを得たらActivatedを発火する」仕組みに乗せることで、クリックとドラッグ開始判定を両立させた
  - **設定ダイアログの可変セパレータ**: カテゴリツリーとコンテンツの間、プロンプト一覧と編集フォームの間の区切り線が固定幅だったのをドラッグでリサイズ可能にした。`PaneManager`のペイン分割で既に使っていた「太い透明な当たり判定＋中央に1pxの線」という`GridSplitter`をそのまま流用できたため、`Theme.cs`に`Theme.Splitter(bool vertical)`として汎用化し、両方の画面で共有
  - **プロンプト一覧の行デザイン変更**: それまでの「(*) 頭文字 プロンプト名 (💛)」という接頭辞方式をやめ、`DataTemplate`ベースの行に変更。プロンプト名を左詰め、既定/お気に入りの状態を右詰めのアイコンで表示し、非アクティブ時は輪郭のみ(☆/♡)、アクティブ時は塗りつぶし(★/♥)になるようにした。既定は金色、お気に入りはピンク系の色を使い、選択行のアクセントカラー(青)と重なっても視認できる配色にした(当初アクセントカラーと同じ青を検討したが、選択行と被って見えなくなるため変更)
  - **既知の制約**: ドラッグ&ドロップ(タイル移動)とタイトルバークリックによるアクティブ化は、実装・ビルド確認とUI Automationによる静的な状態確認までは行ったが、実際のマウス操作によるインタラクティブな動作確認はできていない。理由は、このセッションでのテスト時にユーザーが実機を同時に操作していることが確認され(フォアグラウンドウィンドウが本アプリ以外になっていた)、合成マウスイベント(`mouse_event`/カーソル位置操作)を使うとユーザーの操作を妨害する恐れがあったため、意図的に見送った。設定ダイアログの可変セパレータ、プロンプト一覧の新デザインは、UI Automationの`InvokePattern`(実カーソルを動かさない)と`PrintWindow`によるスクリーンショットで見た目のみ確認済み

- ✅ **M14: ターミナル内の色情報が失われる不具合の修正、タイルドラッグ時のカーソル表示改善、設定ダイアログと本体ウィンドウのタイトルバー配色統一**（2026-09-18 完了）
  - **ターミナルの色が白黒になる不具合の修正（重大なバグ）**: `AnsiPalette.Resolve`が、XTerm.NETのREADMEに書かれている「色モード: 0=既定色, 1=256色パレット, 2=RGB」という説明をそのまま信じて実装されていたが、実際にリンクされているXTerm.NET 1.2.0のランタイムでは`XTerm.Common.ColorMode`列挙体は`Palette256 = 0` / `RGB = 1`の2値しかなく、「既定色（未設定）」は独立したモード値ではなく、Palette256モードの中で有効なインデックス範囲(0-255)の外側にある番兵値（前景色は256、背景色は257。`AttributeData.Default`をリフレクションで実際に呼び出して確認）で表現されていた。この食い違いにより、実際のコマンドプロンプト等が出す標準的なANSI 16色・256色パレット色(モード値0で来る、全体の大半を占める)は`Resolve`の`switch`で`_ => fallback`に落ちて既定色（グレー/白）にされてしまい、実質「色がほぼ全部消える」状態になっていた。さらに24bit RGBトゥルーカラー(実際のモード値は1)も、旧コードでは`case 2`だけを見ていたため正しく解釈されていなかった。`Resolve`を「mode==RGBならRGB値としてデコード、それ以外(Palette256)は値が0-255の範囲内ならパレット色、範囲外(256/257)なら既定色」という実際の挙動に合わせて書き直した。**教訓: サードパーティライブラリのREADME/ドキュメントの説明文だけを信じず、疑わしい挙動が出たら実際のランタイム値をダンプして検証すること**（このバグは、ConPTY経由でcmd.exe/PowerShellをspawnし、実際にANSIエスケープシーケンスを送って`AttributeData`の値をリフレクションで直接確認する、UIに一切触れない独立したテストハーネスを組んで原因を特定した）
  - **タイルドラッグ時のカーソル表示**: タイルのタイトルバーにマウスを乗せると掴めることが分かるよう`Cursor = Cursors.Hand`を設定。また、実際にドラッグ中はWPFの既定のドラッグカーソル(ドロップ不可な位置で出る「禁止」マーク含む)を`GiveFeedback`イベントで`e.UseDefaultCursors = false`にして抑制し、常に同じ手のマークのカーソルを表示し続けるようにした（有効なドロップ先でなくても「禁止」マークにはならない）
  - **設定ダイアログのタイトルバー配色統一**: それまで`DarkTitleBar`はOS標準の`DWMWA_USE_IMMERSIVE_DARK_MODE`だけを使っており、Windows標準の汎用ダークモードの色（本体ウィンドウの自前タイトルバー`#1A1A1A`とは微妙に異なる色）になっていたため、本体ウィンドウと設定ダイアログで「別のダークテーマ」のように見えていた。Windows 11(ビルド22000以降)の`DWMWA_CAPTION_COLOR`/`DWMWA_TEXT_COLOR`を追加で設定し、本体ウィンドウのタイトルバー色(`#1A1A1A`背景・`Gainsboro`文字)と一致させた。古いWindowsではこの2つの呼び出しは失敗するだけで無視され、従来通りの汎用ダークモード表示にフォールバックする
  - 実機で、設定ダイアログのタイトルバーが本体ウィンドウの自前タイトルバーと同系色になったことをスクリーンショットで確認済み。色の修正自体は、UIに触れない独立テストハーネスで実際にconhost/PowerShell経由のANSI出力を`XTerm.Terminal`に流し込み、修正後のロジックで赤・緑・トゥルーカラーが正しいRGB値に解決されることを確認済み。タイルドラッグ時のカーソルの見た目は実機のマウス操作でのインタラクティブな確認はできていない(M13と同様の理由)

- ✅ **M15: ドラッグ中カーソルの挙動修正、タイル1枚だけでの×ボタンの挙動統一、タイトルバーへのセパレータ・バージョン表示追加、IME全角入力の不具合修正**（2026-09-19 完了）
  - **ドラッグ中カーソルをVSCode流に修正**: M14で「ドラッグ中も常にHandカーソルのまま（禁止マークを抑制）」にしたが、これはユーザーの意図と違っていた。正しくは「掴める場所(タイトルバー)にマウスを乗せている間だけHandカーソル、実際にドラッグを開始したらHandカーソルではなくなる」という、VSCode等の一般的なドラッグ挙動。`GiveFeedback`ハンドラでのカーソル上書き(`UseDefaultCursors=false`)を削除し、ドラッグ中はWPF既定のドラッグカーソル(禁止マーク含む)に戻した。Hoverの`Cursor=Cursors.Hand`はそのまま維持
  - **タイルが1枚だけの状態でそのタイルの×ボタンを押した場合の挙動統一**: それまでは`PaneManager.Close`が「唯一残っているペインだった場合は何もしない(no-op)」という実装だったため、最後の1枚のタイルの×ボタンを押しても何も起きなかった。これは本体ウィンドウの×ボタンを押したときの「本当に閉じますか」確認ダイアログ→OKなら全タイルを閉じてアプリ終了、という挙動と食い違っていた。`PaneManager`に`LastPaneCloseRequested`イベントを追加し、唯一のペインを閉じようとしたときはこのイベントを発火、`MainWindow`側で`Close()`(＝本体ウィンドウの×ボタンと同じ確認ダイアログ経由の終了フロー)を呼ぶようにして統一した
  - **タイトルバーへのセパレータ追加**: 画面分割/×ボタン群と設定(ギア)ボタンの間、設定ボタンとお気に入りボタン群の間に、1pxの縦線(`#FF3A3A3A`)を追加してグループを視覚的に区切った
  - **タイトルバーへのバージョン表示**: "TileTerm"の文字の右側（ウィンドウ操作ボタン群の直前）に`vX.Y.Z`形式でバージョンを右詰め表示。アセンブリの`AssemblyInformationalVersionAttribute`は.NET SDKがgitリポジトリ内でビルドすると自動的に`+<コミットハッシュ>`を末尾に付与してしまい長大な文字列になるため、代わりに`Assembly.GetName().Version`(csprojの`AssemblyVersion`由来)のMajor.Minor.Buildのみを使うようにした
  - **IMEでの全角入力ができない不具合の修正**: `TerminalPaneControl.OnPreviewKeyDown`が、Enter/Tab/矢印キー等を無条件に拾って`e.Handled = true`にしターミナルへ直接送っていた。IME変換確定中は、変換候補の選択・確定に使うEnter/Space/矢印キー等はWPF上で通常のキー値ではなく`Key.ImeProcessed`として届くが、この状態を考慮せず処理していたため、変換確定のキー操作がIMEに渡る前にターミナルへの生の入力として横取りされ、IME変換が正しく完了できていなかった可能性が高い。`e.Key == Key.ImeProcessed`の場合は何もせず素通りさせ、IMEが確定した文字列は従来通り`PreviewTextInput`経由でターミナルに送られるようにした
  - **既知の制約**: このバッチのうち、ドラッグ中カーソルの見た目とIME全角入力の修正は、実機での対話的なマウス操作・実際のIME確定操作による検証はできていない(M13以降と同様、ユーザーが実機を同時操作している可能性があり、合成マウス/キーボード入力での自動テストは意図的に避けている)。特にIME修正は、WPFの`Key.ImeProcessed`に関する標準的な既知の挙動に基づく修正であり、コード上の理屈は通っているが実際の日本語入力での動作確認はユーザーによる再テストが必要。タイル1枚×ボタンの統一動作とタイトルバーのセパレータ・バージョン表示は、UI Automationの`InvokePattern`とスクリーンショットで実機確認済み

- ✅ **M16: IME入力の作り直し、ドラッグ中の禁止マーク廃止、タイトルバーのアプリ名削除、設定ダイアログのCLion風デザイン調整**（2026-09-19 完了）
  - **IME（全角入力）**: M15の`Key.ImeProcessed`ガードだけでは「変化がない」と指摘された。UIに触れない検証用のWPFテストアプリを作って調べたところ、素の`FrameworkElement`でも確定文字列自体は`PreviewTextInput`で届く（=M15時点でも「あ」+Enterは入力できていた）が、**変換中の文字がターミナル上に一切表示されず、候補ウィンドウも離れた位置に出る**ため実利用では「入力できない」ように見えていたと判断。`TerminalPaneControl`にカーソルセル位置に重ねた透明な`TextBox`（`_imeBox`）を置き、キーボードフォーカスとキー/テキスト入力ハンドラをここに移した（`TerminalCanvas`は描画専用で`Focusable=false`）。TextBoxはTSFのフルサポートを持つため、変換中の文字がカーソル位置にアンダーライン付きで表示され、候補ウィンドウもキャレット位置に出る。確定文字列は`PreviewTextInput`でターミナルへ送り、TextBox内に残る確定済みテキストは`BeginInvoke`で消す。`TerminalCanvas`は描画の度にカーソルセルの矩形が変わったら`CursorMoved`イベントを発火し、ペイン側がそれで`_imeBox`の位置を追従させる。**ペインのキー入力/フォーカス処理は今後`_imeBox`側にある**（`FocusCanvas()`という名前は互換のため残しているが実体は`_imeBox.Focus()`）
  - **IMEのテスト方法（重要）**: 実機のIMEは`keybd_event`で`VK_KANJI(0x19)`（IMEオン/オフ切替）→`K`→`A`→`Enter`と送ると再現できる（`InputMethod.Current.ImeState`をコードから設定するテスト用アプリも併用）。**必ず直前に`GetForegroundWindow()`が対象ウィンドウであることを確認してから送る**（ユーザーが実機を操作中の場合に他のウィンドウへキーストロークが漏れないため）。IMEのオン/オフ状態はプロセスごとに新規起動するとオフに戻るので、テストの度に`0x19`を1回送ればオンになる（既にオンのプロセスに送るとオフになる点に注意）
  - **ドラッグ中のカーソル**: ドラッグ中に「移動できない範囲では禁止マークになるが、その必要はない」との指摘。`GiveFeedback`で`e.Effects == None`の時だけ`UseDefaultCursors=false`+通常の矢印カーソルにして禁止マークを抑止（有効なドロップ先でのWPF既定カーソルはそのまま）。タイトルバーにマウスを乗せている間だけ`Hand`カーソルなのはM15のまま
  - **タイトルバー**: "TileTerm"のアプリ名テキストを削除し、バージョン表示(`vX.Y.Z`)のみ右詰めで残した
  - **設定ダイアログをCLion（JetBrains）の設定画面に寄せた**（参考画像は`reference/ref_CLion-settings.png`、こちらはライトテーマだが形状・配置・密度を参考にし配色は従来のダークのまま）: ①テキストボックス/ボタンの高さを24pxに統一(`Theme.ControlHeight`)、角丸を3→2に縮小、内部padding削減（従来のテキストボックスは約28px+丸みが強かった）②フォームは「ラベルを上に積む」→「ラベルを左、フィールドを右」の2カラム配置にし、補足説明はフィールド下の小さいグレー文字(`Theme.Hint`)へ③実行ファイルの`参照...`ボタンをフィールド右隣の小さなフォルダアイコンボタン(`Icons.Folder`)に④サイドバー/リストの行は余白なしで端から端までの矩形選択（角丸なし、`ListBox`のPadding=0）、サイドバー背景を`BgList`にして内容領域と区別⑤ヘッダー直下の区切り線を削除⑥チェックボックスをダーク配色の自作テンプレートに(`Theme.CheckBox`、チェック時はアクセント色塗りつぶし+白チェック)⑦下部バーを`OK`(主要ボタン、Enterで実行)/`キャンセル`/`適用`に変更。**`適用`はフォームに未保存の編集がある間だけ有効**、`OK`は未保存の編集があれば適用してから閉じる、`キャンセル`/Escは編集を破棄して閉じる（従来の`保存`/`閉じる`から変更。編集内容の保存タイミングの意味が変わった点に注意。追加/削除/既定/お気に入りの切替は従来通り即時反映）⑧ダイアログを開いた時に既定のプロンプトを選択済みにする
  - **設定ダイアログの既定サイズ**: 740×440 → 920×640（最小700×460）に拡大
  - 確認済み: IMEで「か」「き」の変換中表示と確定、確定後にテキストが残らないこと、設定ダイアログの新デザイン、タイトルバーの表示（いずれもスクリーンショット）。**未確認**: ドラッグ中のカーソル（合成マウス操作を避けているため実機確認できていない）

- ✅ **M17: プロンプトのアイコンを画像方式に変更、設定ダイアログの縦サイズ縮小と名前欄下のセパレータ**（2026-09-19 完了）
  - **アイコンの仕様変更**: これまでの「表示アイコン」(絵文字/短い文字列のテキスト、`ProfileDefinition.Icon`/`DisplayIcon()`)を廃止し、画像アイコンに変更。`ProfileDefinition.IconPath`(null/空=実行ファイルのアイコン)を新設。**`profiles.json`の古い`Icon`項目は読み込み時に無視される**（次回保存で消える）。アイコンの決定ロジックは`ProfileIcons.cs`に集約: ①`IconPath`が画像ファイル(png/jpg/jpeg/bmp/gif/ico/tif/tiff)ならその画像（`.ico`は最大サイズのフレーム、大きい画像は128pxに縮小）②画像以外のファイルなら、そのファイルのシェルアイコン(`SHGetFileInfo`、System.Drawing.Commonへの依存を増やさないためP/Invoke)③未設定なら`Executable`のアイコン（`pwsh`のような裸のコマンド名はPATH/PATHEXTで解決）④どれも取得できなければ名前の頭文字を描いた角丸タイル。結果は(パス,更新日時)でキャッシュ。呼び出し側は`ProfileIcons.Get(...)`で常に`ImageSource`が得られ、`ProfileIcons.CreateImage`で高品質縮小設定済みの`Image`が作れる
  - **表示箇所**: 設定ダイアログのプロンプト一覧（アイコン+名前、右に既定/お気に入り）、フォームの「名前」の右隣の四角いアイコンボタン（クリックで画像/任意ファイルを選ぶダイアログ。カスタム設定時のみ「既定に戻す」リンクが出る。名前/実行ファイル欄の編集に合わせてプレビューが更新される）、タイルのタイトルバー（アイコン+名前）、メインのタイトルバーのお気に入りボタン（**「アイコン画像 名前」**の並び）。従来の「表示アイコン」欄は削除。アイコンの変更も`適用`の未保存編集(dirty)判定に含まれる
  - **設定ダイアログ**: 「名前」行と「実行ファイル」以降の間に横のセパレータを追加。既定の高さを640→540に縮小
  - 確認済み（スクリーンショット）: 一覧/お気に入りバー/タイル見出しでexeのアイコンが表示されること、設定ダイアログの新レイアウト。`ProfileIcons`は単体テストハーネスで、exe・裸のコマンド名・画像ファイル(128pxに縮小)・任意ファイル(シェルアイコン)・存在しない画像(シェルアイコンにフォールバック)・空(文字タイル)を確認。**未確認**: アイコン選択ダイアログ自体の実操作（ネイティブのファイルダイアログのため自動操作していない）

- ✅ **M18: アプリアイコンの組み込み、タイトルバーアイコンの更新、テーマ(ダーク/ライト)切り替え**（2026-09-19 完了）
  - **アプリアイコン**: 別途デザイン案を12弾ほど検討(claude.aiのアーティファクト「TileTerm アイコン案」https://claude.ai/artifact/4qhYF1v3B6rPWWmKPSsfVP に全案が残っている)した末、「グラデーション・オーブ(インディゴ地に4色のぼかしブロブ、細い切れ目で非対称に3分割した円)」を採用。`src/TileTerm/Assets/TileTerm.ico`(16〜256pxの7サイズ、PNG圧縮)を`TileTerm.csproj`の`ApplicationIcon`(exe/タスクバー/Alt+Tab/エクスプローラー)と`MainWindow`の`Icon`に設定。**icoとタイトルバー用PNGは`scripts/make_icon.py`(Pillow)で再生成できる**(小サイズほど切れ目を太くして潰れを防ぐ。デザインを変える時はこのスクリプトの色・ブロブ・切れ目を編集)。タイトルバー左上は`Icons.App()`が`Assets/TileTerm-titlebar.png`(96px)を18DIPで表示する(icoをImageに読むと1フレームを拡縮してぼやけるため別PNGにしている)。従来の2x2タイルのベクター描画は廃止
  - **アイコン候補の保存と差し替え手順**: 最終候補だった12案(**S5-a〜f**=グラデーション・オーブの6配色、**S2-a〜f**=スライス・ディスクの6配色。IDはデザイン検討の名前と同じ)を`src/TileTerm/IconCandidates/<ID>/`に、`TileTerm.ico`+`TileTerm-titlebar.png`のペアで保存済み(現在の採用はS5-aで、`Assets/`のファイルとバイト単位で一致する)。**候補フォルダはビルドに含まれない**(WPFの`Resource`として埋め込まれるのは`Assets/`の2ファイルだけで、候補は通常のファイル扱いなので実行ファイルは太らない)。**差し替え**: `python scripts/make_icon.py --use S2-c`(または候補フォルダの2ファイルを`src/TileTerm/Assets/`へ上書きコピー)→アプリを閉じて再ビルド。**コードの変更は不要**(csprojの`ApplicationIcon`・`MainWindow.xaml`の`Icon`・`Icons.App()`が同じファイル名を参照しているため)。実際にS2-cへ切り替えてビルドし、exeの関連付けアイコンがS2-cになることを確認済み(その後S5-aに戻した)。注意: ①icoとタイトルバー用PNGは必ずセットで差し替える(exe/タスクバー用とタイトルバー用の2か所で使われる)②アイコンはビルド時にexeへ埋め込まれるので再ビルドが必要。Explorerやタスクバーのアイコンキャッシュで古い絵が残る場合は再ログインかexeのコピーで更新される③候補の再生成は`python scripts/make_icon.py`(Pillow必須、引数なし)。色やブロブ位置を変えたい時はスクリプト内の表(`S5`/`S2`)を編集する④配色によってはどちらかのテーマで縁が弱い(モノトーンのe系は明るい背景/暗い背景のどちらかで端が溶ける等)。採用前にライト/ダーク両方のタイトルバーで見ること
  - **テーマ**: 設定ダイアログに「テーマ」カテゴリを追加。ダーク(従来の色そのまま)とライトから、その配色のミニチュア画面(固定色で描画したプレビューカード)を選ぶ。選択は「プロンプト」の編集と同じ扱いで、`適用`/`OK`で反映される(`適用`はプロンプトの未保存編集かテーマの未反映のどちらかがある間だけ有効)。選択は`%AppData%\TileTerm\settings.json`(`{"Theme": "Dark"|"Light"}`)に保存され、次回起動時に復元される(既定はDark)。**再起動なしで**本体・設定ダイアログ・確認ダイアログ・OSタイトルバーがその場で切り替わる。**ライトの色は`reference/ref_CLion-settings.png`(CLionのライトテーマ)から実測**した値で、ウィンドウ/タイトルバー/下部バーが`#F2F2F2`系、カテゴリのサイドバーが青みのグレー`#E6EBF0`、リスト・入力欄が白、選択行が`#2675BF`、区切り線が`#C9C9C9`前後、文字がほぼ黒。最初に作った白基調(`#FAFAFA`のターミナル面など)は「明るすぎる」と指摘されたため調整した
  - **構成**: `ThemePalette.cs`(UIクロームの色定義Dark/Light、および**タイル固定色`TileScheme`**)、`ThemeManager.cs`(現在のテーマ・`Changed`イベント・保存)、`AppSettings.cs`(settings.json)、`Theme.cs`(共有ブラシ+コントロールテンプレート。従来の色定数はここに残っているが値はパレットから入る)、`TitleBarTheme.cs`(旧`DarkTitleBar.cs`を改名。OSタイトルバーの配色)
  - **設計上の要点（ハマった点）**:
    1. **ブラシの更新方式**: 色ごとに共有の`SolidColorBrush`を1つ持ち、テーマ変更時にその`.Color`を書き換える。コードで組み立てるUI(`Icons`、ペインのヘッダー等)はこの共有ブラシをプロパティにそのまま代入する。**ただしStyle/ControlTemplateのSetter値や`FrameworkElementFactory.SetValue`に入れたFreezableは、テンプレートの使用時にfreezeされて以降書き換えられなくなる**ため、テンプレート内では`DynamicResource`(`FrameworkElementFactory.SetResourceReference`、`new Setter(prop, new DynamicResourceExtension(key))`)で参照する
    2. **`ResourceDictionary`に入れたFreezableもfreezeされる**: 共有ブラシをそのまま`Application.Resources`に登録したところ、起動時に「オブジェクト '#FF1E1E1E' は読み取り専用状態」でクラッシュした。リソース側は別インスタンスにして、テーマ変更時は`resources[key] = new SolidColorBrush(...)`でエントリごと差し替える(DynamicResourceが再解決する)。共有ブラシの書き換えとリソースの差し替えの2系統を`Theme.ApplyPalette`が同時に更新する。新しい色を足す時は`ThemePalette`のフィールド、`Theme`の共有ブラシ、(XAML/テンプレートで使うなら)`Theme.Shared`表の3か所を更新する
    3. リソースの登録は`App.OnStartup`で、`base.OnStartup`(StartupUriでMainWindowを生成する)より**前**に行う。XAMLの`DynamicResource`が解決できなくなるため
    4. **列挙型名に`ThemeMode`は使えない**: .NET 9のWPFに`Window.ThemeMode`(評価用API)があり、`Window`派生クラス内で型名が隠されてエラーになる。`ThemeKind`にしている
    5. **タイル(タイトル帯+ターミナル部分)はテーマに関わらず常にダーク**(`TileScheme`: タイトル帯`#252525`/文字`#DCDCDC`、ターミナル背景黒/前景`#E5E5E5`、ANSI16色はxterm標準)。理由: ターミナルは暗い背景が慣例で、Claude Code・vim等のTUIは暗い背景前提の固定色で描くため、明るい背景に切り替えると読めなくなる(最初は「One Half Light」系のライト配色を作ったが、ユーザーの判断でタイルはダーク共通にした)。テーマで変わるのはタイルの**周囲**(ウィンドウ背景・タイトルバー・ペイン間の隙間と分割線・アクティブ枠・ダイアログ)だけ。タイル内のボタン(×/更新)は`Icons.Close/Refresh`の`ink`引数で明るい色を指定している(既定のインクはテーマ追従で、ライト時は暗色になってしまうため)。`AnsiPalette`/`TerminalCanvas`はテーマを参照しない
    6. **OSタイトルバー**(設定/確認ダイアログ): `TitleBarTheme`が`DWMWA_USE_IMMERSIVE_DARK_MODE`を0/1で切り替え、Windows 11では`DWMWA_CAPTION_COLOR`/`DWMWA_TEXT_COLOR`もパレットの色にする。開いている間にテーマが変わっても`Changed`で更新される
  - **既知の制約**: ライトテーマでもタイルは黒いままなので、明るい画面の中に黒い面が大きく残る(意図した仕様)。ターミナルの配色そのものをテーマ連動/ユーザー設定にしたくなった場合は`TileScheme`をパレット化して`AnsiPalette`から参照する形に戻せばよい。プロンプトの頭文字タイル(`ProfileIcons.LetterTile`)はテーマに関わらず従来のグレー地
  - **確認済み**(UI Automation+`PrintWindow`のスクリーンショット): ダークが従来と同じ見た目、設定の「テーマ」ページ、ライトを選ぶと`適用`が有効になり適用で本体・設定・OSタイトルバーが即座に切り替わる、再起動後もライトが維持される、ライト時の確認ダイアログ、ダークへ戻す、ライト時にタイルが暗いまま(タイトル帯の文字・ボタンも読める)で複数分割できること、設定ダイアログのライト配色をCLionの参考画像と見比べて同系統であること。exeの関連付けアイコンとしてオーブが取れること(`ExtractAssociatedIcon`)。アイコンは明るい/暗い背景での見え方を実測(縁の輝度コントラスト比: 暗背景で中央値約5.7、明背景で約2.5〜2.6。シアンの部分が明背景で最も弱い)し、拡大画像で確認したが、どちらでも円形のマークとして識別できる。**未確認**: タスクバー/Alt+Tabでの実際のアイコン表示
  - **UI自動確認のメモ**: 設定ダイアログ等のOwner付きウィンドウはUIAツリー上では本体ウィンドウの**子**として現れる(`RootElement`直下を探しても見つからない)。Windows PowerShell 5.1は**BOMなしUTF-8の`.ps1`を文字化け**させるので、日本語を含むスクリプトはBOM付きで保存する。`SelectionItemPattern.Select()`でリスト項目/ラジオボタンを選べる

- ✅ **M19: 公開の準備（ライセンス・README・ポータブル/インストーラ・リリース自動化）**（2026-09-19 完了）
  - `LICENSE`(MIT、`Copyright (c) 2026 syanmi`)、`THIRD-PARTY-NOTICES.md`(Porta.Pty/XTerm.NET/.NETはいずれもMIT)、`README.md`(**英語**。UIが日本語のみである旨を冒頭に明記している。スクリーンショットは`docs/images/`。個人情報が写らないよう、ポータブル版のdataフォルダにデモ用のプロファイルを置いて撮影した)を追加。ライセンスは`syanmi`名義のMITにしたが、公開前ならいつでも変更できる。`.gitignore`に`artifacts/`・`*.log`・`reference/`などを追加し、`reference/ref_CLion-settings.png`は追跡をやめた(ファイル自体はローカルに残っている)
  - **ポータブル版のデータ保存先**: `AppPaths.cs`を新設し、`TileTerm.portable`マーカーがあるときだけ`data`フォルダ(exeの隣)を使う。`ProfileStore`と`AppSettings`はここを参照する
  - パッケージ作成`scripts/build_release.ps1`、インストーラ`installer/TileTerm.iss`、CI(`.github/workflows/ci.yml`はpush/PRでビルド、`release.yml`はタグでリリース作成)。詳細は前の「リリース」の節を参照
  - **ハマった点**: ①Inno Setupを検証用に入れる時、推測したダウンロードURLはHTMLのページを返した(公式の入手は`winget download --id JRSoftware.InnoSetup`がハッシュ検証つきで確実)②Windows PowerShell 5.1の`ZipFile.CreateFromDirectory`はzipの中のパス区切りが`\`になる③`.ps1`はBOMなしだと日本語が文字化けする(スクリプトはASCIIのみにした)

- ✅ **M20: Velopackのパッケージ/更新基盤、リリース自動化、v0.1.1の公開**（2026-09-19 完了）
  - M19で作ったInno Setup(+自前のポータブルzip)を**Velopackに置き換えた**(公開前だったので、既存の利用者への乗り換え負担がなかった)。理由: 自動アップデートを見据え、インストーラ・ポータブル・更新パッケージ・差分・GitHub Releasesのフィードを一つの仕組みで賄えるため。`Velopack`(NuGet)と`vpk`(ローカルツール)は1.2.0に揃えた。`installer/TileTerm.iss`は削除
  - アプリ側: `Program.cs`(本当のエントリポイント)に`VelopackApp.Build().Run()`、`AppPaths.cs`のポータブル判定をVelopackのものに変更(`TileTerm.portable`マーカーは廃止)、csprojから`AssemblyVersion`/`FileVersion`を削除して版を1か所に
  - リリース自動化: `scripts/release.ps1`(版の更新・コミット・タグ・push)、`scripts/build_release.ps1`(Velopackでパッケージ作成)、`scripts/release_notes.ps1`(コミット件名からノート生成)、`.github/workflows/release.yml`(タグでReleaseを作る)、`ci.yml`(毎pushでビルドとパッケージ作成)。詳細は「リリース」の節
  - **ハマった点**: ①`TestVelopackLocator`の3引数コンストラクタの3番目はルートではなく**パッケージのフォルダ**(ルートを渡すと「インストール済みの全体パッケージ」が見つからず、差分ではなく全体をダウンロードする判断になる)②Windows PowerShell 5.1で`2>$null`付きの外部コマンドは`$ErrorActionPreference='Stop'`だと標準エラー出力で終了扱いになる(`Continue`に緩める)③`Get-Content`(既定のエンコーディング)でUTF-8のJSONを表示すると文字化けして見えるが、ファイル自体はUTF-8で正しい④Pythonで文字列を作る時、パスの`\b`や`\t`が制御文字に化ける(生文字列を使うか、ツールで直接編集する)
  - 確認済み(ローカル): インストーラの静かなインストール→起動→アンインストール(ショートカット・登録情報が消える、`%AppData%`が残る)、新しいSetup.exeの上書きによる更新、ポータブルの起動とデータが`data`(`current`の外)に作られること、0.1.1→0.1.2の差分パッケージ(約0.2MB)の生成と、Velopackの更新チェック→ダウンロードで差分から全体が復元されること
  - **v0.1.1を公開した**(https://github.com/syanmi/TileTerm/releases/tag/v0.1.1 、初めての公開リリース。`scripts\release.ps1 -Version 0.1.1`で版の更新・タグ・pushを行い、`release.yml`がReleaseを作った)。確認済み(GitHub上の実物): CIのパッケージ作成(PowerShell 7と`vpk`を含む)とReleaseワークフローが成功、添付は`TileTerm-win-Setup.exe`・`TileTerm-win-Portable.zip`・`TileTerm-0.1.1-full.nupkg`・フィード(`releases.win.json`・`assets.win.json`・`RELEASES`)・`SHA256SUMS.txt`、リリースノートの日本語が正しい、`releases/latest`が0.1.1、固定URL(`releases/latest/download/TileTerm-win-Setup.exe`)から取得したSetup.exeのSHA256が一致・インストール→起動(0.1.1)→アンインストールが成功、Velopackの`GithubSource`で実際のReleaseに対し「0.1.0のインストールから0.1.1を検出して全体をダウンロードできる」「0.1.1では新しい版なしと判定される」。**未確認**: 差分パッケージがGitHub上で機能すること(差分は2回目以降のリリースで初めて作られる。ローカルでは確認済み)、アプリ内の更新機能(未実装)
  - `release.ps1`は使い捨てのcloneで試験してから本番に使った。この試験で、ヘルパー関数名`Git`がPowerShellの大文字小文字を区別しない名前解決で外部コマンド`git`を隠し、自分自身を呼び続けて止まる不具合を見つけて直した(`Invoke-Git`に改名)。**PowerShellのスクリプトでは、関数名を外部コマンドと同じにしない**

## 未実装（コンセプト自体には含まれない、品質・UX向上の候補）

コンセプトで挙げられていた機能（画面分割・複数セッション・任意のコンソールアプリをプロファイルで設定可能）はM2で一通り動作するところまで実装済み。以下は今後の改善候補:

- レンダリングのパフォーマンス最適化（同一属性の連続セルをまとめて1回のDrawTextにする、色/フォントのBrush/Typefaceをキャッシュする等。現状は逐セル毎回生成でCPU負荷が高め）
- Bold/Underline/Inverse 以外のテキスト属性（Italic, Dim, Strikethrough, Blink, Overline）の描画
- ウィンドウリサイズ以外のきっかけ（DPI変更等）でのセルメトリクス再計算
- ペインの分割比率・レイアウトの保存/復元（現状は再起動すると単一ペインに戻る）
- プロファイルへのアイコン/色設定、複数ウィンドウ対応、タブ機能

## 名前の由来・検討経緯

「Terminal」+「Tile」（画面をタイル状に分割してセッションを配置する機能）から命名。
命名検討時には RLogin, Wireshark, MultiTerm (SDL/RWS の用語管理ツールと衝突するため回避), termbox-go 等の既存ソフトウェア名を調査した上で、機能が直感的に伝わりやすいことを優先してこの名前に決定した。
